// dataUtils.js

export function createData(name, desciption, numemployee, id) {
        return { name, desciption, numemployee, id };
}

export function createDataEmployee(name, desciption, numemployee, department, status, id) {
        return { name, desciption, numemployee, department, status ,id };
}
